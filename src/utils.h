#ifndef _UTILS_H_
#define _UTILS_H_

#include <vector>
using namespace std;


float min(float a, float b);

float max(float a, float b);

template <typename T>
void swap(int i, int j, vector<T> &v);

#endif
